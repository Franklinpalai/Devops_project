#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/network
- name: Create docker network for 3-tier app
  docker_network:
    name: app-network
    state: present

- name: Create proxy network for httpd and frontend
  docker_network:
    name: proxy-network
    state: present