name: CD - Build and Push Docker Images

on:
  push:
    branches:
      - main
      - develop
      - test_hope # 🚀 Suivi direct de ta branche actuelle

jobs:
  # ==========================================
  # 1. JOBS DE BUILD ET PUSH DOCKER
  # ==========================================

  build-and-push-backend:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login --username ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Build image and push backend
        uses: docker/build-push-action@v6
        with:
          # 🌟 Vrai chemin direct vers ton API
          context: ./backend
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-simple-api:latest
          push: true

  build-and-push-database:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login --username ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Build image and push database
        uses: docker/build-push-action@v6
        with:
          # 🌟 Vrai chemin direct vers ta bdd
          context: ./database
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-database:latest
          push: true

  build-and-push-httpd:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login --username ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Build image and push httpd
        uses: docker/build-push-action@v6
        with:
          # 🌟 Vrai chemin direct vers ton serveur Apache
          context: ./httpd
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-httpd:latest
          push: true

# ==========================================
  # 2. JOB DE DÉPLOIEMENT CONTINU (ANSIBLE)
  # ==========================================

  deploy:
    needs: [build-and-push-backend, build-and-push-database, build-and-push-httpd]
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Configuration propre de la clé SSH (On nettoie la sortie stderr)
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ANSIBLE_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 400 ~/.ssh/id_rsa
          # 🌟 Le '2>/dev/null' permet de cacher les logs d'info qui faisaient paniquer GitHub
          ssh-keyscan franklin.kalfadangpalai.takima.school > ~/.ssh/known_hosts 2>/dev/null

      # Installation d'Ansible
      - name: Install Ansible
        run: |
          sudo apt update
          sudo apt install -y ansible

      # Installation de la collection Docker requise pour Ansible
      - name: Install Ansible Docker collection
        run: |
          ansible-galaxy collection install community.docker

      # Exécution du Playbook en restant dans le working directory
      - name: Run Ansible playbook
        working-directory: ./ansible
        run: |
          ansible-playbook -i inventories/setup.yml playbook.yml --vault-password-file <(echo "${{ secrets.ANSIBLE_VAULT_PASSWORD }}")